import { observer } from 'mobx-react-lite'
import React from 'react'

interface IResultPartProps {
}

const ResultPart: React.FC<IResultPartProps> = () => {
  return null
}
export default observer(ResultPart)
